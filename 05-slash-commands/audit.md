# /audit

Make `03-your-product` match reality. Never rewrite `AGENTS.md` or `01-start-here/AGENTS.md`.

# Need

- The real repo (scaffold or later code)
- Current files in `03-your-product`

# Do

- After Vercel import, write the live URL and real build commands into `03-your-product/architecture.md`
- After scaffold, fix folder names and stack versions so they match the tree
- After a job, fix any file in `03-your-product` that the code made untrue
- Leave both `AGENTS.md` files as the index

# Stop

- Do not invent a new product
- Do not replace `AGENTS.md` with a stack dump
- Do not write feature code
